
= link_to 'New Department', new_department_path, class: 'btn btn-primary pull-right'

%h1 Listing departments
%table.table.table-hover.table-striped
  %thead
    %tr
      %th Name
      %th Description
      %th
      %th
      %th

  %tbody
    - @departments.each do |department|
      %tr
        %td= department.name
        %td= department.description
        %td= link_to_show(department)
        %td= link_to_edit(edit_department_path(department))
        %td= link_to_destroy(department)

%br
