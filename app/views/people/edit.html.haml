%h1= t('views.entites.people.edit.title')

= render 'form'

%div.mt-3
  = link_to_show(@person)
  \|
  = link_to (fa_icon 'reply' ,text: t('views.general.links.back') ), people_path, class: 'btn btn-secondary'
