
= link_to 'New Post', new_post_path, class: 'btn btn-primary pull-right'

%h1 Listing posts
%table.table.table-hover.table-striped
  %thead
    %tr
      %th Code
      %th Name
      %th
      %th
      %th

  %tbody
    - @posts.each do |post|
      %tr
        %td= post.code
        %td= post.name
        %td= link_to_show(post)
        %td= link_to_edit(edit_post_path(post))
        %td= link_to_destroy(post)

%br
