=breadcrumb_tag do
  %li.breadcrumb-item
    = link_to Worker.model_name.human(count: 'other'), workers_path
  %li.breadcrumb-item
    = link_to "#{@worker.id}", @worker
  %li.breadcrumb-item.active
    = t('views.entites.worker.new.title')

%h1= t('views.entites.workers.edit.title')

= render 'form'

%div.mt-3
  = link_to_show(@worker)
  \|
  = link_to (fa_icon 'reply' ,text: t('views.general.links.back') ), workers_path, class: 'btn btn-secondary'
