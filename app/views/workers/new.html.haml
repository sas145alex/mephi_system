=breadcrumb_tag do
  %li.breadcrumb-item
    = link_to Worker.model_name.human(count: 'other'), workers_path
  %li.breadcrumb-item.active
    = t('views.entites.workers.new.title')

%h1= t('views.entites.workers.new.title')

= render 'form'

= link_to (fa_icon 'reply' ,text: t('views.general.links.back') ), workers_path, class: 'btn btn-secondary'
