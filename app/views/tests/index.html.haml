
= link_to 'New Test', new_test_path, class: 'btn btn-primary pull-right'

%h1 Listing tests
%table.table.table-hover.table-striped
  %thead
    %tr
      %th Test string
      %th Test int
      %th
      %th
      %th

  %tbody
    - @tests.each do |test|
      %tr
        %td= test.test_string
        %td= test.test_int
        %td= link_to_show(test)
        %td= link_to_edit(edit_test_path(test))
        %td= link_to_destroy(test)

%br
